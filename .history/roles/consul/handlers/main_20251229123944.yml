---
- name: Reload consul
  ansible.builtin.systemd:
    name: consul
    state: reloaded
  become: true

- name: Restart consul
  ansible.builtin.systemd:
    name: consul
    state: restarted
  become: true
